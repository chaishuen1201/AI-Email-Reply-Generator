import Home from "./pages/Home.jsx";

/**
 * App
 * ---
 * Root component of the application. Kept intentionally minimal so that
 * routing/layout providers can be added here later without touching
 * page-level components.
 */
function App() {
  return <Home />;
}

export default App;